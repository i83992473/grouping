# Public config only (Vite exposes VITE_* to the browser).
# Do not put database passwords or RDS connection strings here.

# Base URL for a future HTTP API that talks to RDS (Lambda, ECS, etc.).
# Example: https://api.example.com
VITE_API_BASE_URL=
